//猜数字
#include <bits/stdc++.h>
#define maxn 1010
int main()
{
	int n, a[maxn], b[maxn];
	int kase = 0;
	while(scanf("%d",&n) == 1 && n){//n = 0 时结束
		printf("Game: %d\n", ++kase);
		for(int i = 0; i < n; i++) scnaf("%d",&a[i]);
		
	}
}